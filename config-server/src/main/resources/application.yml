server:
  port: 8086

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8671/eureka

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/a962332430/spring-cloud-study/
          username: a962332430
          password: 921103chen
          search-paths: config-repo
      label: master




